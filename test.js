const a = [1, 2];

const o = {};

[o.a, o.b] = a;

[] = match(targ)
({color: 'black', left: { color: 'red', right: { color: 'red'}}, right: { color: 'red' }},
  ({ left: a, right: b}) => [ a, (c.left = b, c)])
(0);
